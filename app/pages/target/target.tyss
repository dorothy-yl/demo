/* Base Styles */
.container {
  display: flex;
  flex-direction: column;
  background-color: #1A1D26; /* Darker theme base */
  min-height: 100vh;
  color: #FFFFFF;
  padding: 0;
  box-sizing: border-box;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
}

.background-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: -1;
  object-fit: cover;
  opacity: 0.4; /* Dim background for better contrast */
}

.overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  background: linear-gradient(180deg, rgba(10, 14, 39, 0.8) 0%, #1A1D26 100%);
}

/* Header */
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 88rpx 0 40rpx 0; /* Converted to rpx */
  position: relative;
  z-index: 10;
}

.back-btn {
  width: 60rpx;
  height: 60rpx;
  position: absolute;
  margin-top: 106rpx;
  left: 46rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
  transition: opacity 0.3s ease;
}

.back-btn:active {
  opacity: 0.6;
}

.back-icon {
  width: 18rpx;
  height: 36rpx;
}

.header-title {
  font-size: 36rpx;
  font-weight: 400;
  color: #FFFFFF;
  margin-top: 106rpx;
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  white-space: nowrap;
  text-align: center;
}

.header-title.thin {
  font-weight: 300; /* Thin font style */
  letter-spacing: 2rpx;
}

.header-placeholder {
  width: 60rpx;
  height: 50rpx;
  visibility: hidden;
}

/* Tab Bar */
.tab-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx;
  margin: 45px 30px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 32rpx;
  position: relative;
  z-index: 10;
}

.tab-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  padding: 24rpx 0;
  transition: all 0.3s ease;
  border-radius: 24rpx;
  border: 2rpx solid transparent;
}

.tab-item.active {
  background: linear-gradient(180deg, rgba(74, 144, 226, 0.15) 0%, rgba(74, 144, 226, 0.05) 100%);
  border: 2rpx solid #4A90E2;
  box-shadow: 0 8rpx 24rpx rgba(74, 144, 226, 0.1);
}

.tab-text {
  font-size: 28rpx;
  color: #8E8E93;
  transition: color 0.3s ease;
}

.tab-item.active .tab-text {
  color: #FFFFFF;
  font-weight: 500;
}

/* Tab Underline Animation */
.tab-underline {
  position: absolute;
  bottom: 16rpx;
  width: 40rpx;
  height: 4rpx;
  background-color: #4A90E2;
  border-radius: 2rpx;
  animation: slideIn 0.3s ease forwards;
}

@keyframes slideIn {
  from { width: 0; opacity: 0; }
  to { width: 40rpx; opacity: 1; }
}

/* Picker Container */
.picker-container {
  width: 360rpx;
  height: 600rpx;
  margin: 60rpx auto;
  position: relative;
  border: 4rpx solid rgba(0, 0, 0, 0.1); /* Black border with 10% opacity */
  border-radius: 16rpx;
  box-sizing: border-box;
  display: flex;
  justify-content: center;
  align-items: center;
}

.target-picker {
  width: 100%;
  height: 100%;
  background: transparent;
}

.picker-item {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48rpx;
  color: #8E8E93; /* Gray for unselected */
  transition: all 0.3s ease;
}

.picker-item.active {
  font-size: 72rpx; /* 1.5x size */
  color: #FFF; /* Red highlight */
  font-weight: bold;
}

.unit-overlay {
  position: absolute;
  right: -80rpx; /* Position outside or inside? */
  top: 50%;
  transform: translateY(-50%);
  font-size: 32rpx;
  color: #8E8E93;
  white-space: nowrap;
}

/* Adjust unit position if it needs to be inside the border.
   User said "Retained elements: maintain... '公里' identifier style".
   The provided image usually shows the unit next to the selected number.
   If I put it at right: -80rpx, it's outside the border.
   If the border is around the *numbers*, this is correct.
   If the border is around the *whole thing*, I should put unit inside and adjust widths.
   "maintain original container green border" implies the border was there.
   I'll put it inside.
*/
.unit-overlay {
  position: absolute;
  right: 40rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 32rpx;
  color: #8E8E93;
  pointer-events: none;
}

/* Empty State Hint */
.empty-hint {
  width: 100%;
  padding: 0 40rpx 20rpx;
  box-sizing: border-box;
  opacity: 0;
  transform: translateY(20rpx);
  transition: all 0.3s ease;
  height: 40rpx; /* Reserve space */
  z-index: 10;
  text-align: center;
}

.empty-hint.show {
  opacity: 1;
  transform: translateY(0);
}

.empty-hint-text {
  font-size: 28rpx;
  color: #8E8E93;
  text-align: center;
  display: block;
}

/* Footer Button */
.footer {
  width: 100%;
  padding: 40rpx 60rpx 80rpx;
  box-sizing: border-box;
  z-index: 10;
}

.start-btn {
  width: 100%;
  background: linear-gradient(135deg, #4A90E2 0%, #357ABD 100%);
  color: #FFFFFF;
  border-radius: 60rpx;
  height: 112rpx;
  font-size: 36rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  box-shadow: 0 8rpx 40rpx rgba(74, 144, 226, 0.3);
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.start-btn::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 60rpx;
  box-shadow: 0 0 30rpx rgba(74, 144, 226, 0.6);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.start-btn-hover {
  transform: scale(0.95);
  box-shadow: 0 4rpx 20rpx rgba(74, 144, 226, 0.2);
}

.start-btn:active {
  transform: scale(0.9);
}

.start-btn:hover::after {
  opacity: 1;
}
